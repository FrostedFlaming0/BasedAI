{
  "name": "@basedai/client",
  "version": "0.1.0",
  "description": "BasedAI TypeScript client — submit prompts, manage spending, query subnets.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "vitest run",
    "lint": "eslint src --ext .ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "viem": "^2.9.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "eslint": "^8.57.0",
    "typescript": "^5.4.0",
    "vitest": "^1.4.0"
  },
  "files": ["dist", "README.md"],
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
